def sample_function(a: int, b: int) -> int:
    """
    This is a sample function that adds two integers.
    It exists to be a fixture for testing the code audit workflow.
    """
    result = a + b
    return result
